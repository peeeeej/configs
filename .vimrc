set number " shows line numbers
set incsearch " highlights searched characters as you type 
set hlsearch " highlights searched words
set showmatch " if you cursor over a character pair, like (, the matching ) is highlighted
set tabstop=4 " when indenting with '>', use 4 spaces width
set shiftwidth=4 " On pressing tab, insert 4 spaces
set expandtab " insert space characters whenever the tab key is pressed
"  optional stuff below
"  set spell spelllang=en_us " spellcheck on
"  syntax enable " enable syntax highlighting
"  set autoindent " indent when moving to the next line while writing code
"  set cursorline " show a visual line under the cursor's current line
"  let python_highlight_all = 1 " enable all python syntax highlighting features
